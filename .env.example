# Node environment: development, production, test
NODE_ENV=production

# Server
PORT=8080
WEBSOCKET_PORT=8081
CORS_ORIGIN=*
HOST=0.0.0.0

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=gopine
DB_PASSWORD=gopinepassword
DB_DATABASE=gopine
DB_SCHEMA=public
DB_SSL=false

# Authentication
JWT_SECRET=change_this_to_a_secure_random_string_for_your_lan_environment
JWT_EXPIRATION=24h

# Application settings
LOG_LEVEL=info
MAX_QUEUE_SIZE=1000
JOB_RETENTION_DAYS=30
HEARTBEAT_TIMEOUT_SECONDS=180

# LAN Environment
LAN_MODE=true
ADMIN_PASSWORD=admin_password_change_this